// Wap to check a No. evem or odd 
let n = +prompt("Enter A number");
alert((n%2==0)?`Yes ${n} is even`: `Yes,${n} is odd `);
// WAP 3 integer and check amount then weather

// i all three are equal
// ii. any two are equal
// iii. all are diffrent

// and print suitable message according to it.

// WAP 2 integer and check the grater Number

let j = +prompt("Enter Num 1");
let k = +prompt("Enter Num 2");

alert(((j>=k)?`Num 1 is greater ${j}`:`Num 2 is greater ${k}`));

// Wap to check greater Node. from 3 numbers 
